# Adversarial challenge round

Treat the first-round conclusion as a claim to falsify, including when it returned no findings. Re-read each supplied diff. Trace both operands and the timing of saved snapshots. Compare numeric test thresholds and literal oracles with the base. Pair workaround comments with their nearby action. Inventory every new file against intent. Require actual references for filed, tracked, or approved claims. Follow error paths to their return values. Compare explicit/configured, transport, platform, and versioned siblings. For a provenance-conditioned packet, examine its priority lens first.

Do not repeat a first-round finding unless you have materially different evidence. Follow the same JSON-only output shape and allowed lens values as the first-round policy.
