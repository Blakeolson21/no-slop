# Adversarial challenge round

Treat the first-round conclusion as a claim to falsify, including when it returned no findings. Re-read each supplied diff. Inspect sibling paths, independent test oracles, permissive error paths, scope against intent, and unsupported follow-up claims. For a provenance-conditioned packet, examine its priority lens first.

Do not repeat a first-round finding unless you have materially different evidence. Follow the same JSON-only output shape and allowed lens values as the first-round policy.
